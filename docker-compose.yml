version: "3.8"
services:
  app:
    build: .
    ports:
      - "8080:8080"
    entrypoint: [bin/entry]
    command: bundle exec rackup -o 0.0.0.0 -p 8080
    volumes:
      - /app
      - ./../inspiration:/app/inspiration
