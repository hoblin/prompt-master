#!/bin/bash
set -e
# set env
export RACK_ENV=production
# update repo
git pull origin main --ff-only
# migrate database
rake db:create
rake db:migrate
# sync app
rake app:sync
# merge files
rake app:merge

exec "$@"