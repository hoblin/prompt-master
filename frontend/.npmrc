# Supply chain protection - disable preinstall/postinstall scripts
# This prevents Shai-Hulud style attacks that run malicious code during npm install
# If a legitimate package needs scripts, run: npm install --ignore-scripts=false
ignore-scripts=true
